# Документация меню

Проект содержит структурированную документацию по меню и блюдам. Исходный материал — файл **MenuInitial.PDF** (запрос на меню завтраков с критериями). Вся дальнейшая разбивка и описание хранятся в **Markdown (.md)**.

---

## Структура проекта

```
Menu/
├── README.md                 ← этот файл (обзор структуры)
├── MenuInitial.PDF           ← исходный документ-основа
│
├── .cursor/
│   ├── agents/
│   │   ├── shef.md           ← субагент «Шеф» для меню и блюд
│   │   ├── economist.md      ← расчёт стоимости и закупки
│   │   └── menu-developer.md ← разработка сайта меню
│   └── rules/
│       └── menu-docs.mdc     ← правила для релевантной работы с документацией
│
├── scripts/
│   └── generate-manifest.js  ← генерация manifest + копирование docs в web/content
│
├── web/                      ← веб-приложение для просмотра меню
│   ├── index.html
│   ├── css/style.css
│   ├── js/app.js
│   └── content/              ← копия docs (меню, блюда, подсказки, data); создаётся скриптом
│       └── manifest.json
│
└── docs/                     ← вся документация меню
    │
    ├── source/               ← что извлечено из исходника
    │   └── menu-initial-summary.md   краткое содержание PDF, критерии запроса
    │
    ├── criteria/             ← аудитория и ограничения
    │   └── audience-and-constraints.md   семья, время, бюджет, лактоза и т.д.
    │
    ├── menus/                ← разделы меню (по приёмам пищи)
    │   ├── zavtraki.md       завтраки (28 вариантов)
    │   ├── obedy.md          обеды (супы на 3 дня, 6 вариантов)
    │   ├── poldnik.md        полдник (ПП-перекусы, 12 вариантов)
    │   ├── uzhiny.md         ужины (28 вариантов)
    │   ├── nedelya-1.md … nedelya-4.md   меню на 4 недели (завтрак + обед + полдник + ужин)
    │   ├── zakupki-na-nedelyu.md         индекс закупок (ссылки на недели)
    │   └── zakupki-nedelya-1.md … zakupki-nedelya-4.md   закупки по неделям (продукты и количество)
    │
    ├── tips/                 ← подсказки и заготовки
    │   └── zagotovki.md      шпаргалка по заготовкам (из PDF + дополнения)
    │
    ├── data/                 ← данные для генерации (рейтинги и т.д.)
    │   ├── ratings.json     рейтинги блюд (1–5); учитываются при генерации меню
    │   └── README.md         описание формата
    │
    └── dishes/               ← карточки блюд (74 шт.: 28 завтраков, 6 супов, 12 полдников, 28 ужинов)
        └── README.md         формат карточки и связь с меню
```

---

## Назначение папок и файлов

| Путь | Назначение |
|------|------------|
| **docs/source/** | Текст и суть, извлечённые из MenuInitial.PDF (без полного парсинга PDF). Основа для критериев и первого раздела меню. |
| **docs/criteria/** | Аудитория (семья 5 человек, дети, лактоза), лимит по времени (≤40 мин), бюджет, требования к молочному. Используются при разработке и описании блюд. |
| **docs/menus/** | Разделы меню: завтраки, при необходимости — обеды, ужины и т.д. В каждом файле — список блюд и ссылки на карточки в `dishes/`. |
| **docs/dishes/** | Одна карточка = один .md-файл: название, описание, состав по компонентам, порции, время, аллергены, подача. Связаны с разделами в `menus/`. |

---

## Формат хранения

- Вся новая и разбитая информация хранится в **формате Markdown (.md)**, если не попросят иного.
- Исходный PDF не изменяется и остаётся единственным «сырым» источником.

---

## Как пользоваться

1. **Критерии и контекст** — смотреть `docs/criteria/audience-and-constraints.md` и при необходимости `docs/source/menu-initial-summary.md`.
2. **Разделы меню** — `docs/menus/zavtraki.md` (28 завтраков), `docs/menus/obedy.md` (обеденные супы на 3 дня), `docs/menus/poldnik.md` (полдник: ПП-перекусы, часть впрок), `docs/menus/uzhiny.md` (28 ужинов), `docs/menus/nedelya-1.md`–`nedelya-4.md` (меню на 4 недели по дням: завтрак, обед, полдник, ужин).
3. **Закупки на неделю** — `docs/menus/zakupki-na-nedelyu.md` (индекс) и `zakupki-nedelya-1.md` … `zakupki-nedelya-4.md`: продукты с количеством на 5 человек по каждой неделе; из меню недели — ссылка на соответствующий файл закупок.
4. **Конкретное блюдо** — искать карточку в `docs/dishes/` по ссылке из меню или по имени файла.
5. **Разработка и описание меню/блюд** — можно привлекать субагента «Шеф» (`.cursor/agents/shef.md`): он ориентирован на описание меню, блюд и разбивку на компоненты. При генерации меню на неделю Шеф учитывает рейтинги из `docs/data/ratings.json` (см. `docs/data/README.md`).

---

## Веб-приложение и динамическая подгрузка

- **Запуск:** раздайте папку `web/` по HTTP (загрузка контента по `fetch` не работает при открытии `file://`). Например: `npx serve web` или `python -m http.server 8080` из папки `web`. Затем откройте в браузере указанный адрес (например `http://localhost:3000`). Контент подгружается из `web/content/` по запросу (меню и карточки в .md).
- **Обновление контента:** после добавления или правки файлов в `docs/` выполните `node scripts/generate-manifest.js`. Скрипт обновит `web/content/` (копия меню, блюд, подсказок и `data/`) и пересоберёт `manifest.json`. Новые и изменённые файлы появятся в приложении без правки кода.
- **Рейтинги блюд:** на карточке блюда можно поставить оценку (1–5 звёзд). Оценки хранятся в localStorage; кнопка «Скачать рейтинги (ratings.json)» выгружает их в файл. Сохраните его в `docs/data/ratings.json` — тогда при следующей генерации меню (Шеф) рейтинги будут учтены (предпочтение блюдам с высоким рейтингом).
- **Мобильные устройства:** вёрстка ориентирована на открытие с телефона: бургер-меню в шапке, крупные области нажатия, горизонтальный скролл таблицы недели, поддержка safe-area (вырезы экрана). Можно добавить на главный экран (PWA-подобное поведение через meta apple-mobile-web-app-capable).
- **Публикация (доступ из любого места):** см. **[docs/publish.md](docs/publish.md)** — пошагово: Netlify, Vercel, Cloudflare Pages, GitHub Pages. В репозитории уже есть `netlify.toml`, `vercel.json` и workflow для GitHub Actions; после подключения к выбранному сервису сайт будет собираться и открываться по постоянному URL с телефона и компьютера.

---

## Дальнейшее развитие

- Завтраки и ужины из PDF разобраны: 20 + 21 блюдо; добавлены обеды (6 супов на 3 дня), полдники (12 ПП-перекусов: печенье, кисель, смузи, гренки, йогурт, творог, фрукты и т.д.) и новые блюда для 4-й недели. Всего 28 завтраков, 6 супов, 12 полдников, 28 ужинов. Карточки в `docs/dishes/`, заготовки в `docs/tips/zagotovki.md`.
- Обеды: суп варится в воскресенье (на Пн–Ср) и в среду (на Чт–Сб), воскресный обед — по выбору семьи. Окрошка и холодник — на тёплый сезон.
- Полдник: часть перекусов готовится впрок на 2–3 дня (овсяное печенье с финиками/какао, бананово-овсяное печенье, овсяные батончики, кисель); напитки чередуют — какао, травяной чай, кисель. Йогурт БЛ, творог, фрукты — покупное с нарезкой.
- При появлении новых общих правил или аудитории — расширять или добавлять файлы в `docs/criteria/`.

Общая схема документации сохраняется: **исходник → критерии → разделы меню → карточки блюд**.
